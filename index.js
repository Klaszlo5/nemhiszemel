window.addEventListener("load",init)
function $(elem){
    return document.querySelectorAll(elem);
}
function init(){
    var piros=document.querySelector(".sarga");
    var lampa=$(".lampa")
    var sarga=document.querySelector(".sarga");
    var zold=document.querySelector(".zold");
    sarga.getElementsByClassName.opacity="20%";
    zold.getElementsByClassName.opacity="20%";
    setInterval(() => {
        sarga.getElementsByClassName.opacity="100%";
    },1000);
    setInterval(() => {
        if (allapot){

        }

    },1000);
}
